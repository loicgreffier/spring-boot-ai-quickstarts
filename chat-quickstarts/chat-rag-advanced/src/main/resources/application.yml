spring:
  ai:
    ollama:
      base-url: "http://localhost:11434"
      chat:
        model: "qwen3.5:9b"
        temperature: 0.1
        think: false
      embedding:
        model: "qwen3-embedding:0.6b"
      init:
        pull-model-strategy: "ALWAYS"
        timeout: "15m"
    vectorstore:
      pgvector:
        initialize-schema: true
  datasource:
    url: "jdbc:postgresql://localhost:5432/postgres"
    username: "postgres"
    password: "changeme"
logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: "DEBUG"
          rag:
            preretrieval:
              query:
                transformation:
                  TranslationQueryTransformer: "DEBUG"
                expansion:
                  MultiQueryExpander: "DEBUG"
